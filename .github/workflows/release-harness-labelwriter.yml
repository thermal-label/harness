name: Release harness-labelwriter

# Builds the labelwriter harness app to a static bundle, zips dist/,
# and attaches it as a Release artifact named
# `harness-labelwriter-<tag>.zip`. The docs site CI's
# `pull-harness-bundles` step pulls the `latest`-tagged artifact and
# serves it under thermal-label.github.io/harness/labelwriter/.
#
# Triggered by tags matching `harness-labelwriter-v*`. Run locally
# (no auto-tag in this repo) by:
#   git tag harness-labelwriter-v0.1.0
#   git push origin harness-labelwriter-v0.1.0
#
# This workflow does NOT trigger releases automatically — the
# maintainer cuts tags by hand.

on:
  push:
    tags:
      - 'harness-labelwriter-v*'

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v5
        with:
          version: 9

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build harness-labelwriter
        run: pnpm --filter @thermal-label/harness-labelwriter build

      - name: Determine artifact name
        id: artifact
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          echo "name=harness-labelwriter-${TAG}.zip" >> "$GITHUB_OUTPUT"

      - name: Zip dist/
        working-directory: apps/harness-labelwriter/dist
        run: zip -r "../../../${{ steps.artifact.outputs.name }}" .

      - name: Upload as GitHub Release artifact
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.artifact.outputs.name }}
          generate_release_notes: true
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
